{
	"name": "zod-form1",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"build": "tsdown",
		"login": "wrangler login",
		"login-no-browser": "wrangler login --browser false",
		"deploy": "wrangler deploy --minify",
		"format": "oxfmt",
		"lint": "oxlint",
		"check": "oxfmt && oxlint --fix",
		"schema1": "bun script/schema1.ts",
		"schema2": "bun script/schema2.ts"
	},
	"dependencies": {
		"hono": "^4.12.32",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/node": "^26.1.2",
		"tsdown": "^0.22.14",
		"typescript": "^7.0.2",
		"wrangler": "^4.114.0"
	}
}
